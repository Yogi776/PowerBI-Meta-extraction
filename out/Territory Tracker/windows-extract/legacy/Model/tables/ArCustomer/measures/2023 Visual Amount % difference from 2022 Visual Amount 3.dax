
VAR __BASELINE_VALUE = SUM('GenTransaction'[2024 Visual Amount])
VAR __VALUE_TO_COMPARE = SUM('GenTransaction'[2025 Visual Amount])
RETURN
	IF(
		NOT ISBLANK(__VALUE_TO_COMPARE),
		DIVIDE(__VALUE_TO_COMPARE - __BASELINE_VALUE, __BASELINE_VALUE)
	)